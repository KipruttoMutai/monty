#include "monty.h"
/**
 * br_stack - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
*/
void br_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	s_data.lifi = 0;
}

/**
 * br_queue - prints the top
 * @head: stack head
 * @counter: line
 * Return: Null
*/
void br_queue(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	s_data.lifi = 1;
}
